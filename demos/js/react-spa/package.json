{
  "name": "react-spa",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "web-vitals": "^1.0.1"
  },
  "devDependencies": {
    "webpack-vite-serve": "link:../../.."
  },
  "scripts": {
    "dev": "SKIP_PREFLIGHT_CHECK=true react-scripts start",
    "dev:vite": "wvs start -f react",
    "build": "react-scripts build",
    "build:vite": "wvs build -f react",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
